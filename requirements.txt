# Web Framework
# Flask 3.1.x - Latest stable version with security updates
# Using ~= allows patch updates (3.1.x) but prevents breaking changes
Flask~=3.1.2

# CORS support for development
Flask-CORS~=4.0.0
